@model ProjectInfoResult

<a asp-route-id="@Model.Id" class="btn @((Model.TotalWorkItems > 0) ? "btn-info" : "btn-light")" asp-page="Projects">@Model.Name</a>
<br/>
<partial name="Items/_ProjectInfo" model="Model"/>