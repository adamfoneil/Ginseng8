@model AppInfoResult

<a class="btn @((Model.TotalWorkItems > 0) ? "appButton" : "btn-light")" asp-route-appId="@Model.Id" asp-page="Org" data-app-id="@Model.Id">
    @Model.Name
</a>
<br/>
<partial name="Items/_ItemInfo" model="Model"/>
